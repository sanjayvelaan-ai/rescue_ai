{
  "name": "rescue-ai-drone-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build",
    "install:all": "npm --prefix frontend install",
    "dev:frontend": "npm --prefix frontend run dev"
  }
}
